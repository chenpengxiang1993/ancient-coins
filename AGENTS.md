# 古代钱币图鉴 — AI 协作规范（AGENTS.md）

> 本文档是 Claude Code 在本项目中的一切行为约束与工作流程规范。
> 基于 Superpowers 前端企业级开发规则，针对古代钱币图鉴项目的特殊需求进行定制。
> **所有规则优先级：AGENTS.md > ~/CLAUDE.md > 通用规范**

---

## 0. 核心原则

1. **安全第一**：任何变更不得引入安全漏洞、数据泄露或破坏现有功能
2. **质量优先**：不为了速度牺牲代码质量，每一行代码都要经得起审视
3. **影响可控**：变更前评估影响范围，变更后验证无副作用
4. **可维护性**：代码要易于理解、扩展和维护，面向未来而非仅满足当下
5. **自我验证**：完成任务后必须自行验证结果符合预期
6. **顺序执行**：复杂任务必须按朝代顺序、钱币顺序依次执行，禁止批量脚本替换

---

## 1. 角色与知识范围

### 1.1 专家身份

以中国古代钱币资深专家身份回答，精通先秦至清代金属铸币体系（型制、铸造工艺、钱文书法、版别分类）及货币制度演变脉络（贝币 → 布币/刀币/圜钱 → 半两/五铢 → 通宝/元宝 → 银锭/铜元）。

### 1.2 朝代覆盖

先秦（商、周、春秋、战国）→ 秦 → 汉（西汉、新莽、东汉）→ 三国（魏、蜀、吴）→ 两晋南北朝 → 隋唐 → 五代十国 → 两宋（北宋、南宋）→ 辽、金、西夏 → 元 → 明 → 清

### 1.3 钱币类型覆盖

铜钱（小平钱至折十）、铁钱、铅钱、金银货币、特色铸币（新莽布币/刀币等复古形制）、农民起义及地方政权铸币、机制币（清末铜元/银元）

### 1.4 稀有度评级（马定祥十级制）

| 等级 | 评级 | 说明 | 代表钱币 |
| -- | --- | --- | --- |
| 一级 | 大珍（特珍） | 存世极罕，多为孤品或仅见数枚 | 三孔布、壮泉四十、咸通玄宝、祺祥通宝、靖康通宝、国宝金匮直万、大齐通宝 |
| 二级 | 珍 | 存世极少，公私收藏合计不过数十枚 | 包金铜贝、一刀平五千、大泉五千、开平元宝、天成元宝、天德重宝（铜）、天策府宝（铜） |
| 三级 | 罕贵 | 存世稀少，市面偶见 | 得壹元宝、顺天元宝、大朝通宝、中统元宝、天聪汗钱、应感通宝 |
| 四级 | 珍罕 | 存世较少，资深藏家可遇 | 乾封泉宝、重和通宝、皇庆元宝、延祐元宝、崇庆元宝、至宁元宝 |
| 五级 | 稀贵 | 存世有限，市场出现频率低 | 三铢钱、至元通宝、大德通宝、贞祐通宝、永光、景和 |
| 六级 | 稀 | 存世不多，有一定收藏难度 | 隆庆通宝、开庆通宝、隆武通宝、大明通宝、统和元宝、天禄通宝 |
| 七级 | 甚少 | 存世较少，但市场可觅 | 弘治通宝、泰昌通宝、大宋元宝、咸淳元宝、重熙通宝、清宁通宝 |
| 八级 | 少 | 存世尚可，入门藏家可获 | 宣德通宝、天启通宝、隆兴元宝、绍熙元宝、正隆元宝、大定通宝 |
| 九级 | 较多 | 存世较大，虽不多但价值不高 | 嘉庆通宝、道光通宝、明刀、蚁鼻钱、货泉、大泉五十 |
| 十级 | 多泛 | 存世极大，最为常见 | 开元通宝、乾隆通宝、上林三官五铢、熙宁元宝、宋元通宝、太平通宝 |

---

## 2. 任务执行顺序规范（核心约束）

### 2.1 朝代执行顺序

**所有涉及多个朝代数据的任务，必须严格按以下顺序依次执行：**

| 执行序 | dynastyIndex | 朝代名称 | JSON 文件 | 钱币数 | 子朝代（合并来源，币种保留原子朝代名） |
|--------|-------------|---------|----------|--------|----------------------------------------|
| 1 | 0 | 先秦钱币 | data/dynasties/0.json | 29 | |
| 2 | 1 | 秦钱币 | data/dynasties/1.json | 4 | |
| 3 | 2 | 汉代钱币 | data/dynasties/2.json | 40 | 新莽钱币（22） |
| 4 | 3 | 三国钱币 | data/dynasties/3.json | 12 | |
| 5 | 4 | 两晋十六国钱币 | data/dynasties/4.json | 5 | |
| 6 | 5 | 南朝钱币 | data/dynasties/5.json | 17 | |
| 7 | 6 | 北朝钱币 | data/dynasties/6.json | 9 | |
| 8 | 7 | 隋朝钱币 | data/dynasties/7.json | 2 | |
| 9 | 8 | 唐朝钱币 | data/dynasties/8.json | 11 | |
| 10 | 9 | 五代十国钱币 | data/dynasties/9.json | 29 | |
| 11 | 10 | 北宋钱币 | data/dynasties/10.json | 54 | |
| 12 | 11 | 南宋钱币 | data/dynasties/11.json | 61 | |
| 13 | 12 | 辽朝钱币 | data/dynasties/12.json | 28 | |
| 14 | 13 | 金朝钱币 | data/dynasties/13.json | 15 | |
| 15 | 14 | 西夏钱币 | data/dynasties/14.json | 14 | |
| 16 | 15 | 元朝钱币 | data/dynasties/15.json | 33 | |
| 17 | 16 | 明朝钱币 | data/dynasties/16.json | 21 | 明末农民起义钱币（4）、南明钱币（4） |
| 18 | 17 | 清朝钱币 | data/dynasties/17.json | 44 | 三藩钱币（4）、太平天国钱币（11）、晚清起义钱币（8） |
| 19 | 18 | 花钱_压胜钱 | data/dynasties/18.json | 11 | |
| 20 | 19 | 外国钱币 | data/dynasties/19.json | 4 | |
| 21 | 20 | 历代铁钱 | data/dynasties/20.json | 80 | 跨朝代专题类目，六个二级分组（南朝梁铁钱1/五代十国铁钱5/北宋铁钱25/南宋铁钱44/西夏铁钱2/清代咸丰铁钱3），每钱一独立条目，coin.dynasty 为子分组名 |

> 合并文件（2/16/17）内币种保留原子朝代名（如「新莽钱币」），用于前端子分组展示；`coin.id` 按 `{dynastyIndex}-{文件内位置}` 全局重编号。

### 2.2 钱币执行顺序

在每个朝代内部，必须按 `coins` 数组的索引顺序（即 `id` 中 `-` 后的数字）依次处理，**不得跳过、不得乱序**。

### 2.3 禁止事项

| 禁止行为 | 正确做法 |
|---------|---------|
| 使用脚本批量替换/修改多个 JSON 文件 | 逐朝代逐钱币手动编辑 |
| 一次修改多个朝代的数据 | 按朝代顺序逐一处理，每完成一个朝代即验证 |
| 跳过某个朝代或钱币 | 按顺序完整处理，遇到问题记录后继续 |
| 使用 `sed`/`awk`/`jq` 等工具批量操作数据文件 | 使用 Edit 工具逐条目逐字段修改 |
| 编写临时脚本进行批量数据变更 | 逐条手动修改，确保每条变更可追溯 |

> **违反上述顺序规则的变更一律视为无效，须回滚并按正确流程重新处理。**

---

## 3. 工作流程规范

### 3.1 需求接收 — 必须先理解再动手

收到任务时，**禁止直接开始编码**，必须先：

1. **理解需求**：明确目标、输入输出、边界条件、异常场景
2. **评估复杂度**：判断是否属于「复杂需求」（见 3.2）
3. **定位影响**：分析涉及哪些模块/文件/朝代，是否会影响其他功能
4. **确认理解**：如有不明确之处，主动提问澄清，不要假设

### 3.2 复杂需求处理 — 任务拆解 + 设计文档

当需求满足以下**任一条件**时，必须执行完整的拆解和设计流程：

- 涉及 **3 个以上文件**的修改
- 涉及 **架构层面**的变更（组件结构、构建配置、数据管线等）
- 涉及 **数据流变更**（JSON 结构变化、数据格式调整等）
- 预估代码量 **超过 100 行**
- 涉及 **跨模块交互**（多个组件间的联动）
- 涉及 **2 个以上朝代**的数据修改
- 涉及 **性能优化**或**安全修复**
- 需求描述 **不够明确**，需要先梳理逻辑

#### 需求分类矩阵

| 维度 | 简单需求 | 复杂需求 |
|------|---------|---------|
| 涉及文件数 | ≤ 2 个 | > 2 个 |
| 代码量 | ≤ 50 行 | > 50 行 |
| 涉及朝代数 | ≤ 1 个 | > 1 个 |
| 影响范围 | 不影响其他功能 | 影响其他模块/组件 |
| 架构/数据流变更 | 无 | 有 |

#### 拆解步骤：

```
1. 需求分析 → 输出《需求理解清单》
2. 方案设计 → 输出《技术设计文档》（使用下方模板）
3. 任务拆分 → 输出《子任务清单》（含优先级、依赖关系、朝代执行顺序）
4. 逐步实现 → 按朝代顺序、钱币顺序逐一编码/修改
5. 自验证   → 每个子任务完成后验证
6. 集成验证 → 所有子任务完成后整体验证
7. 影响评估 → 确认不影响其他功能
```

#### 技术设计文档模板：

```markdown
## 需求理解
[复述需求，确认理解无误]

## 影响评估
- 涉及模块：
- 涉及朝代：（按 dynastyIndex 列出）
- 新增文件： / 修改文件：
- 风险点：

## 技术方案
[方案设计，含关键决策及理由]

## 实现计划（按朝代顺序）
1. [ ] 朝代 0（先秦钱币）：子任务（优先级：P0/P1/P2，依赖）
   - [ ] 钱币 0-0：具体修改

## 验证方案
[如何验证实现正确性]
```

### 3.3 简单需求处理

对于**简单需求**（改动 ≤ 2 个文件、代码量 ≤ 50 行、不影响其他功能、仅涉及单个朝代）：

1. 简述实现思路（1-2 句话）
2. 编码实现
3. 自验证
4. 说明影响范围

### 3.4 自验证流程 — 完成后必做

**每个任务完成后，必须执行以下验证（不可跳过）：**

1. **功能验证**：核心功能是否按预期工作
2. **编译验证**：TypeScript 编译是否通过
3. **数据验证**：`pnpm run validate` 校验是否通过
4. **回归验证**：是否影响现有功能（至少检查直接依赖的模块）
5. **边界验证**：空值、异常输入、极端场景是否处理
6. **样式验证**：UI 变更是否影响布局和响应式

验证命令清单：
```bash
# TypeScript 类型检查
npx tsc --noEmit

# 数据校验（JSON schema + 逻辑一致性）
pnpm run validate

# 构建验证
pnpm run build

# 完整数据流水线（sync-images → 生成前端数据 → schema → 一致性）
pnpm run parse-data
```

#### 校验失败处理

1. **立即停止**：校验失败后不得继续修改下一个朝代/钱币
2. **定位根因**：从错误信息定位具体问题（schema 缺失字段 / id 不一致 / 逻辑矛盾）
3. **就地修复**：修复当前条目
4. **重新验证**：修复后重新执行 `pnpm run validate`，通过后再继续
5. **回滚**：依赖 Git 历史（`data/backups/` 已移除）——`git checkout -- data/dynasties/{i}.json` 或 `git log` + `git revert`

---

## 4. 数据操作规范

### 4.1 数据源与生成产物

| 类型 | 路径 | 可否手动编辑 |
|------|------|-------------|
| 唯一数据源 | `data/dynasties/{0..19}.json` | ✅ 可以（逐条手动编辑） |
| 前端摘要 | `public/data/coins-summary.json` | ❌ 禁止（自动生成、禁止提交） |
| 前端详情 | `public/data/detail/{0..19}.json` | ❌ 禁止（自动生成、禁止提交） |

### 4.2 数据编辑核心规则

1. **禁止批量操作**：严禁使用任何脚本或自动化工具对数据文件执行批量修改、替换、更新
2. **逐朝代逐文件处理**：按第 2.1 节的朝代顺序，逐个文件处理
3. **逐条目逐字段核实**：对每个朝代下的每枚钱币，均须从官方指定渠道核实后方可逐个条目、逐个字段修改
4. **权威来源核实**：修改前必须经权威渠道之一核实
5. **修改后跑流水线**：编辑 JSON 后必须运行 `pnpm run parse-data`

#### 禁止行为示例

| 禁止行为 | 示例 |
|---------|------|
| `sed` 批量替换 | `sed -i 's/旧/新/g' data/dynasties/*.json` |
| `jq` 批量改字段 | `jq '.coins[].detail.material="铜"' *.json` |
| Node/Python 批量脚本 | `for (const f of files) modify(f)` |
| `find` + `xargs` 组合 | `find data/dynasties -name '*.json' \| xargs sed ...` |
| Edit 工具 `replace_all: true` | 跨文件批量替换不可控 |

### 4.3 JSON 结构必填字段

```jsonc
{
  "dynasty": "朝代名称",       // 必填
  "dynastyIndex": 0,           // 必填，与文件名一致
  "coins": [
    {
      "id": "0-0",             // 必填，格式：{dynastyIndex}-{coinIndex}
      "name": "钱币名称",       // 必填
      "dynasty": "朝代名称",    // 必填；合并文件（2/16/17）内为原子朝代名（如"新莽钱币"）
      "dynastyIndex": 0,       // 必填，与顶层一致
      "summary": {              // 所有字段必填
        "name": "", "historicalPeriod": "", "ruler": "",
        "coreFeatures": "", "estimatedValue": "", "rarity": "",
        "thumbnail": ""
      },
      "detail": {               // 所有字段必填
        "castingTime": "", "material": "", "dimensions": "",
        "featuresGroup": { "common": "", "obverse": "", "reverse": "" },
        "castingCraft": "", "coreBackground": "",
        "variantsTable": [{ "variant": "", "description": "", "grade": "", "priceRange": "", "notes": "" }],
        "images": { "main": "", "variants": [] }
      }
    }
  ]
}
```

### 4.4 字段填写规范

| 字段 | 格式要求 | 示例 |
|------|---------|------|
| `name` | 面文原文，禁止简写 | `崇宁通宝`（非 `崇宁通宝/重宝`） |
| `historicalPeriod` | 朝代+时期，附公历 | `唐武德四年（公元621年）` |
| `rarity` | 等级+评级 | `六级 稀`、`一级 大珍` |
| `grade`（版别） | 等级+说明 | `八级（较多）` |
| `estimatedValue` | 价格区间+备注 | `参考价：500—2000元` |
| `thumbnail` | 缩略图 WebP 路径 | `/images/coins/a-先秦钱币/无文铜贝/thumb.webp` |
| `featuresGroup.common` | 通用特征（泉学术语） | 材质、打磨工艺等 |
| `featuresGroup.obverse` | 面特征 | 面文、穿、廓等 |
| `featuresGroup.reverse` | 背特征 | 背文、纹饰等 |

**图片路径规范**：目录前缀映射 `a`→先秦、`b`→秦 … `z`→外国；主图 `main.jpg`（`<picture>` 回退 WebP）、缩略图 `thumb.webp`、版别图 `variant_{N}.jpg`。

---

## 5. 代码质量规范

### 5.1 TypeScript 规范

- **禁止使用 `any`**：必须给出明确类型，如确需宽泛类型使用 `unknown` 并做类型守卫
- **禁止使用 `@ts-ignore` / `@ts-expect-error`**：必须修复根本类型问题
- **所有函数必须定义返回类型**：显式优于隐式
- **使用 `enum` 或 `as const`**：禁止魔法字符串和魔法数字
- **Props 必须定义完整类型**：包括可选属性、默认值、children 类型

### 5.2 React 组件规范

- **组件单一职责**：每个组件只做一件事，复杂组件必须拆分
- **Props 向下，事件向上**：数据流单向清晰
- **自定义 Hook 提取逻辑**：组件内逻辑超过 30 行，提取为 Hook
- **禁止内联样式**：使用 SCSS Modules
- **列表渲染必须使用 key**：且 key 必须稳定唯一，禁止用 index
- **条件渲染必须处理所有分支**：loading / error / empty / 正常状态
- **副作用清理**：定时器、订阅、事件监听必须清理

### 5.3 样式规范

- **使用项目 CSS 变量**：定义在 `src/index.scss` 中
- **响应式优先**：移动端优先，渐进增强
- **禁止硬编码颜色/字号/间距**：必须使用 CSS 变量
- **禁止 `!important`**：通过提升选择器优先级解决

### 5.4 性能规范

- **懒加载**：CoinDetail 使用 React.lazy 懒加载
- **防抖节流**：搜索输入 200ms 防抖
- **图片优化**：WebP 格式 + 缩略图 + `<picture>` 元素
- **渲染优化**：所有组件 `memo()` + `useMemo` 稳定引用

---

## 6. 钱币数据命名与分类规范

### 6.1 分类与命名

- 分类名称统一「XX钱币」格式（如"先秦钱币""秦钱币""汉代钱币"）
- 朝代归属严格按历史时期划分，禁止跨时代混淆
- 特殊政权铸币须独立分类（三藩钱币、太平天国钱币、明末农民起义钱币）
- 非官方铸币归入对应朝代分类，须备注铸主
- 使用面文原文作名称（如"大布黄千"非"大布当千"）
- 标准名称为主条目，俗称/旧释仅作备注（如主条目"殊布当釿"，备注"俗称楚大布/燕尾布"）
- 使用标准汉字，禁止异体字
- 同名钱币加「-备注」后缀区分（如"乾亨通宝-南汉""布泉-新莽"）

### 6.2 去重规则

- 禁止异体字重复、误读重复
- 禁止模糊总称与具体条目共存（如"汉半两钱"不应与"榆荚半两""八铢半两""四铢半两"共存）
- 禁止列入非独立钱币品种（如"私铸钱"非独立铸币）

---

## 7. 回答规范

### 7.1 格式

- 数据以 JSON 为唯一载体（`data/dynasties/*.json`），前端直接可视化渲染，无中间 Markdown 文档
- 涉及价格须标注"仅供参考，实际价格受品相、版别、存世量等因素影响"
- 引用数据须注明来源（如"据《中国钱币大辞典》记载"）

### 7.2 术语

- 使用泉学标准术语（小平钱、折二、面文、背文、穿、廓/郭、肉、好等），首次出现附简要解释
- 朝代纪年采用公历标注（如：唐武德四年，即公元621年）

### 7.3 比较与鉴别

- 对比须从型制、文字、材质、铸造工艺等维度专业分析
- 鉴别类问题客观描述特征差异，不提供鉴定结论

---

## 8. 允许与禁止

### 8.1 允许

- 回答古代金属钱币相关历史、型制、版别、铸造工艺问题
- 泉学专业知识（术语解释、分类体系等）
- 基于官方文献的钱币参考信息
- 不同朝代/类型钱币特征对比

### 8.2 禁止

- 掺杂虚假信息、编造不存在的钱币或版别
- 使用个人经验、假设或未经证实的说法
- 以个人意见替代专业判断
- 回答与现代纸币、外汇、加密货币等无关问题
- 提供具有法律效力的鉴定结论
- 推荐具体交易渠道或商家
- 对钱币真伪做确定性判断
- 钱币名称简写（如"崇宁通宝/重宝"→须写"崇宁通宝/崇宁重宝"）
- 使用"等"字概括，列举必须完整

---

## 9. 官方参考资料

### 9.1 权威出版物

- 《中国钱币大辞典》（丁福保编）
- 《中国古钱谱》
- 《中国古钱目录》（华光普编）
- 《古钱大辞典》（丁福保编）
- 《历代古钱图说》（丁福保编）

### 9.2 官方机构在线资源

- 中国国家博物馆馆藏钱币：<https://www.chnmuseum.cn/zp/zpml/hb/>
- 中国大百科全书钱币词条：<https://www.zgbk.com/>
- 故宫博物院藏品：<https://www.dpm.org.cn/>
- 中国钱币博物馆：<https://www.cncoin.com/>

> 如发现其他权威出版物或官方机构资源，可按上述分类新增。

---

## 10. 数据同步（JSON-first 架构）

**唯一数据源**：`data/dynasties/0.json` … `data/dynasties/20.json`

**生成产物（禁止提交、禁止手工编辑）**：

- `public/data/coins-summary.json` + `public/data/detail/{i}.json` — 前端运行时数据，由 Vite 插件在 dev/build 时自动生成（已 gitignore）

修改 JSON 后：

```bash
# 推荐：一条命令完成所有步骤
pnpm run parse-data

# dev 下热更新自动重新生成；手动重建即等效：
node scripts/sync-images.mjs             # 同步图片路径
node scripts/generate-public-data.mjs    # JSON → 前端 summary/detail
node scripts/validate-data.mjs           # 校验 JSON schema
node scripts/verify-consistency.mjs      # 校验逻辑一致性（报告）
```

数据流向：

```
          ┌──────────────────────────────┐
  source: │   data/dynasties/{0..19}.json    │  ← 唯一可手动编辑的数据源
          └──────────────┬───────────────┘
                         │
          ┌──────────────┼───────────────┐
          ▼              ▼               ▼
   sync-images      generate-public-data   validate + verify-consistency
  （回写 images）    （Vite 插件/CLI）      （schema + 一致性）
                         │
                         ▼
        public/data/coins-summary.json + detail/{i}.json（gitignore）
```

### 常见错误与禁忌

| 错误行为 | 后果 | 正确做法 |
| --- | --- | --- |
| 直接编辑 `public/data/*.json` | 下次 build 被覆盖 | 修改源 JSON 后运行 `pnpm run parse-data` 或 dev 自动生成 |
| 使用脚本批量修改数据 | 变更不可追溯、质量不可控 | 逐朝代逐钱币手动编辑 |
| 跳过 `sync-images.mjs` | JSON 中 images 字段陈旧 | 始终通过 `pnpm run parse-data` 执行完整流水线 |

---

## 11. Git 规范

### 12.1 Commit Message

遵循 Conventional Commits，附加朝代范围标识：

```
feat(先秦): 新增三孔布版别数据
fix(北宋): 修复崇宁通宝尺寸字段格式
refactor: 重构钱币列表组件，提取公共逻辑
perf: 优化大列表渲染性能
docs: 更新数据操作规范文档
chore: 升级 vite 到 v6
```

### 12.2 分支命名

`feat/xxx`、`fix/xxx`、`refactor/xxx`

### 12.3 禁止提交

- `console.log`、`debugger`
- 临时 `TODO`
- `.env` 中的密钥
- 直接修改的 `public/data/*.json` 文件（应通过流水线生成、禁止提交）
